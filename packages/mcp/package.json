{
  "name": "@ai-productivity-tracker/mcp",
  "version": "0.0.0",
  "private": true,
  "description": "Thin stdio MCP bridge: translates 5 MCP tools to daemon HTTP calls. Bundled into cli; not published directly.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist .turbo .cache *.tsbuildinfo"
  },
  "dependencies": {
    "@ai-productivity-tracker/hook-core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  }
}
