{
  "name": "@ai-productivity-tracker/hook-core",
  "version": "0.0.0",
  "private": true,
  "description": "Cursor / Claude Code hook entry logic, sentinel storage, stop-check anti-forgery checks. Bundled into cli; not published directly.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rimraf dist .turbo .cache *.tsbuildinfo"
  }
}
