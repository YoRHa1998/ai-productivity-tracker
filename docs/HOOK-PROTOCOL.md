# HOOK PROTOCOL

> Cursor `afterAgentResponse` / `stop` Hook 与 Claude Code `Stop` / `UserPromptSubmit` Hook 的输入输出协议描述。
> **待 Phase 2 hook-core 迁入后补齐**。

源协议描述见 `instant-web-tools/packages/ai-productivity-hook-core/src/hook.ts` 与 `stop-check.ts`。
